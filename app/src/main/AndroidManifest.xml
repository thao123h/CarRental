<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Cho phép truy cập Internet -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />



    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.CarRental"
        android:networkSecurityConfig="@xml/network_security_config"
        android:usesCleartextTraffic="true"
        tools:ignore="ExtraText">

        <activity
            android:name=".activities.HomeActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".activities.CarListActivity"
            android:exported="true" />
        <!-- Add these activities inside <application> tag in AndroidManifest.xml -->

        <activity
            android:name=".activities.BookingListActivity"
            android:label="@string/title_my_trips"
            android:theme="@style/Theme.AppCompat.Light.NoActionBar"
            android:exported="false" />

        <!-- Booking Detail Activity -->
        <activity
            android:name=".activities.BookingDetailActivity"
            android:label="@string/title_booking_detail"
            android:theme="@style/Theme.AppCompat.Light.NoActionBar"
            android:parentActivityName=".activities.BookingListActivity"
            android:exported="false"
            tools:targetApi="26">
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value=".activities.BookingListActivity" />
        </activity>


    </application>

</manifest>
